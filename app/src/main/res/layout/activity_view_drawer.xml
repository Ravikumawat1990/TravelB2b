<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        layout="@layout/app_bar_view_drawer"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/colorPrimary"
        android:fitsSystemWindows="true"
        android:scrollbarThumbVertical="@color/colorPrimary"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        app:itemBackground="@color/colorPrimary"
        app:itemIconTint="@color/menu_selector"
        app:itemTextColor="@color/menu_selector"

        app:menu="@menu/activity_view_home_drawer" />

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view2"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:fitsSystemWindows="true"
        android:scrollbarThumbVertical="@color/colorPrimary"
        app:headerLayout="@layout/nav_header_view_home"
        app:itemIconTint="@color/menu_selectorsec"
        app:itemTextColor="@color/menu_selectorsec"
        app:menu="@menu/activity_view_home_drawersec" />


</android.support.v4.widget.DrawerLayout>
